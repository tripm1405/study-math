<button onClick="upsert('<%= data?.newId %>')">Tạo</button>
<table>
    <thead>
    <tr>
        <th></th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    <% for (const _class of (data?.classes || [])) { %>
        <tr>
            <td><%= _class?.code %></td>
            <td>
              <td>
                <button class="btn btn-secondary" onClick="upsert('<%= _class?._id %>')"><i class="fas fa-edit"></i></button>
                <button class="btn btn-danger" onClick="deleteLesson('<%= _class?._id %>')"><i class="fas fa-trash-alt"></i></button>
              </td>
            </td>
        </tr>
    <% } %>
    </tbody>
</table>

<script>
  function upsert(id) {
    window.location.href = `/classes/${id}`;
  }
  async function del(id) {
    await axios.delete(`http://localhost:5500/classes/${id}`);
    location.reload()
  }
</script>