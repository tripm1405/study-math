<nav>
    <ul class="pagination">
        <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
            <a class="page-link" href="#" onclick="K.Search.onSendSearch('<%= model %>', <%= currentPage - 1 %>)"><</a>
        </li>
        <% for (let i = 1; i <= totalPages; i++) { %>
            <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                <a class="page-link" href="#" onclick="K.Search.onSendSearch('<%= model %>', <%= i %>)"><%= i %></a>
            </li>
        <% } %>
        <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
            <a class="page-link" href="#" onclick="K.Search.onSendSearch('<%= model %>', <%= currentPage + 1 %>)">></a>
        </li>
    </ul>
</nav>